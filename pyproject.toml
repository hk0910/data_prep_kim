[project]
name = "data-prep"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Heather Kim", email = "heatherekim@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
data-prep = "data_prep:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"
